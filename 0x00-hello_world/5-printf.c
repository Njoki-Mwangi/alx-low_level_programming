#include <stdio.h>
/**
 *main - Entry point
 *
 * This function prints a message to the console and retuens 0 on success
 *
 * Return: always 0 (success)
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
